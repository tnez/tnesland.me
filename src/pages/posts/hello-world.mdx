---
layout: 'blog-post'
title: 'Hello World'
---

Hello World!

```ts
/**
 * This is just a test to see if code looks pretty.
 *
 * @param {Object} params
 * @param {boolean} params.foo the foo
 * @param {number} params.baz the baz
 */
 function thing(params: { foo: boolean, baz: number }): number {
   return foo ? 42 : baz
 }
```

And then here is some quote:

> Wherever you go, there you are.

Yay!
